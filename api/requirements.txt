# Dashboard-only dependencies. The scraper's extras (httpx, beautifulsoup4)
# are deliberately absent: the dashboard reads the database and renders HTML,
# it never makes an outbound request, and a smaller function is a faster
# cold start.
fastapi>=0.110
sqlalchemy>=2.0
psycopg[binary]>=3.1
pydantic>=2.6
python-dotenv>=1.0
