# Runtime dependencies for the scheduled scrape job.
# The dashboard has its own pinned set in api/requirements.txt.
httpx[http2]>=0.27
pydantic>=2.6
sqlalchemy>=2.0
psycopg[binary]>=3.1
beautifulsoup4>=4.12
python-dotenv>=1.0
